<%= content_for(:title) do %>
  Visa Countdown
<% end %>

<%= content_for(:description) do %>
  The Schengen area and Turkey are among the countries in which many foreigners can stay with a tourist visa for 90 days in ANY 180 days period. Calculate when you must leave or when you can re-enter.
<% end %>

<div class="container-fluid">
  <div class="row xs-marged">
    <div class="col-xs-12 col-sm-6 col-sm-offset-3 col-md-4 col-md-offset-4">

      <% if @hi %>
        <%= @hi %>
      <% end %>

      <%= form_tag(controller: "welcome", action: "user_details") do %>
        <div class="form-group">
          <label for="citizenship" class="text-uppercase">Citizenship:</label>
          <%= select("resource", "citizenship", @countries, {:include_blank => "Choose one", }, class: "form-control text-uppercase") %>
        </div>
        <div class="form-group">
          <label for="destination" class="text-uppercase">Destination:</label>
          <%= select("resource", "destination", ["Schengen area", "Turkey"], {:include_blank => "Choose one", }, class: "form-control text-uppercase") %>
        </div>
        <button type="submit" class="btn btn-success btn-block text-uppercase"><small>Continue</small></button>
      <% end %>
    </div>
  </div>
</div>