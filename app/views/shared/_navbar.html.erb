<nav class="navbar navbar-default navbar-wagon" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">
        <div class="visible-xs">
          <%= image_tag "logo_no_text.svg", alt: "Logo", title: "Visa Countdown" %>
        </div>
        <div class="hidden-xs">
          <%= image_tag "logo.svg", alt: "Logo", title: "Visa Countdown" %>
        </div>
      </a>
    </div>

    <div id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="dropdown">
            <%= link_to "#", {class: "dropdown-toggle", "data-toggle" => "dropdown", "role" => "button", "aria-expanded" => "false"} do %>
              <i class="fas fa-caret-down"></i> Logged in
            <% end %>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to edit_user_registration_path do %>
                  <i class="fas fa-wrench"></i> Account Settings
                <% end %>
              </li>
            </ul>
          </li>

          <li>
            <%= link_to destroy_user_session_path, method: :delete, class: "btn" do %>
              <i class="fas fa-sign-out-alt"></i>  Log out
            <% end %>
          </li>
        <% else %>
          <li>
            <%= link_to "#", {class: "dropdown-toggle btn", "data-toggle" => "dropdown", "role" => "button", "aria-expanded" => "false"} do %>
              <i class="fas fa-caret-down"></i> Log in
            <% end %>
            <ul class="dropdown-menu" role="menu">
              <li>
                <%= link_to user_twitter_omniauth_authorize_path do %>
                  <i class="fab fa-twitter fa-fw"></i> with Twitter
                <% end %>
              </li>
              <li>
                <%= link_to new_user_session_path do %>
                  <i class="fas fa-sign-in-alt fa-fw"></i> The classic way
                <% end %>
              </li>
            </ul>
          </li>
          <li>
            <%= link_to new_user_registration_path, class: "btn" do %>
              <i class="fas fa-user"></i> Sign up
            <% end %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
